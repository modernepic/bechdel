<p>
  <strong>Title:</strong>
  <%= @movie.title %>
</p>

<p>
  <strong>Link:</strong>
  <%= @movie.link %>
</p>

<h2>Comments</h2>
<%= render @movie.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_movie_path(@movie) %>
<%= link_to 'Back', movies_path %>
